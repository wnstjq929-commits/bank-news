너는 한국 은행권 뉴스 분석 자동화 작업이다. 현재 작업 폴더의 파일만 사용한다.

작업 폴더에 `_work/pending.json` 파일이 있다. 아직 AI 분석이 없는 기사 배열이며, 각 원소는 {id, title, summary, banks, topics} 형태다.
만약 `_work/pending.json` 이 빈 배열이면, 아무 것도 하지 말고 "0건"만 출력하고 종료하라.

pending.json의 각 기사에 대해 한국어로 다음 4가지를 생성한다:
1. summary: 기사의 주요 내용을 빠짐없이 담은 상세 요약. 무조건 줄이지 말고 핵심 사실·배경·수치·관계자 발언·의미를 모두 포함해 4~6문장 이상으로 충실하게. (없는 사실 창작 금지)
2. terms: 기사에 나오는 어려운 용어·약어·전문개념을 골라 각각 쉽게 설명. [{"term":"용어","desc":"쉬운 설명"}] 형태 배열. 어려운 용어가 없으면 빈 배열 [].
3. impact: 이 사안이 해당 은행(또는 은행권)에 미치는 영향을 1~2문장으로 구체적으로. 수익성/건전성/규제리스크/경쟁구도/평판/고객확보 등 어느 측면인지 명시.
4. solutions: 은행이 취할 구체적 대응방안 2~4개를 배열로. "무엇을 어떻게" 수준으로 구체적으로.

그다음 작업 폴더의 기존 `analysis.json`을 읽어라(없으면 빈 객체). 기존 키(id)를 절대 지우지 말고 보존한 채, 새로 생성한 항목을 병합한다. 각 항목은 키=기사 id, 값={summary, terms, impact, solutions} 형태다. 병합 결과를 `analysis.json`에 저장한다.

반드시 유효한 JSON만 저장하라(마크다운 코드펜스나 주석 금지). 완료 후 처리 건수만 한 줄로 출력하라.
